import { CdtecSoNumberDirective } from './cdtec-so-number.directive';

describe('CdtecSoNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CdtecSoNumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
